# Block Club Web Environment
# ==========================
#
# Environment Setup:
# - Local Development: Copy this file to .env.local and fill in dev Supabase credentials
# - Vercel Preview: Configure env vars in Vercel dashboard (dev Supabase project)
# - Vercel Production: Configure env vars in Vercel dashboard (prod Supabase project)
#
# Domain: lakewoodblock.club (prod)
#
# Get credentials from: https://supabase.com/dashboard/project/_/settings/api

# Supabase Project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase Publishable Key (safe for client-side)
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Staff Admin Emails (comma-separated list of emails with system-wide admin access)
STAFF_ADMIN_EMAILS=admin@example.com

# E2E Test User (for Playwright tests)
# Create this user in your dev Supabase: Authentication → Users → Add user
# Email confirmation should be disabled in dev for this to work
E2E_TEST_USER_EMAIL=e2e-test@example.com
E2E_TEST_USER_PASSWORD=your-test-password

# Sentry Error Monitoring
# =======================
# Get DSN from: https://sentry.io/settings/[org]/projects/[project]/keys/

# Required for runtime error reporting (client & server)
NEXT_PUBLIC_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# Required for build-time source map uploads (Vercel/CI only)
SENTRY_ORG=your-org-slug
SENTRY_PROJECT=blockclub-web
SENTRY_AUTH_TOKEN=sntrys_xxx
