{
  "name": "blockclub",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "dev:web": "turbo dev --filter=@blockclub/web",
    "dev:mobile": "turbo dev --filter=@blockclub/mobile",
    "build:web": "turbo build --filter=@blockclub/web",
    "build:mobile": "turbo build --filter=@blockclub/mobile",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "clean": "turbo clean && rm -rf node_modules",
    "test": "turbo test --filter=@blockclub/web --filter=@blockclub/shared",
    "test:unit": "turbo test:unit --filter=@blockclub/web --filter=@blockclub/shared",
    "test:integration": "turbo test:integration --filter=@blockclub/web",
    "test:e2e": "turbo test:e2e --filter=@blockclub/web",
    "test:coverage": "turbo test:coverage --filter=@blockclub/web --filter=@blockclub/shared"
  },
  "devDependencies": {
    "prettier": "^3.2.4",
    "turbo": "^2.3.0"
  },
  "overrides": {
    "react-dom": "18.2.0"
  },
  "packageManager": "npm@10.2.0"
}
